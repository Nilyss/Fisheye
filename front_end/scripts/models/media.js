function Image (id, photographerId, title, image, likes, date, price) {
  return {
    id,
    photographerId,
    title,
    image,
    likes,
    date,
    price,
  }
}

function Video (id, photographerId, title, video, likes, date, price) {
  return {
    id,
    photographerId,
    title,
    video,
    likes,
    date,
    price,
  }
}

